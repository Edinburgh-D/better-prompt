# better-prompt 提示词质量闭环方案

> 目标：不追求复杂 Agent 化，而是用本地测试数据集、完整结果归档、人工复盘和提示词规则迭代，持续提升“优化后的提示词质量”。

## 核心出发点

better-prompt 当前应优先服务普通 AI 用户，而不是专业提示词工程师或企业 SaaS 用户。

因此，产品下一阶段的重点不是插件市场、RLHF、项目工作流或复杂 Agent，而是回答一个更直接的问题：

> 用户输入一句普通话，工具优化后的提示词是否真的更清楚、更可控、更能直接复制使用？

## 当前不做什么

- 不做浏览器插件。
- 不做代码提示词优化入口。
- 不做领域专家插件市场。
- 不做 RLHF 或模型微调。
- 不做复杂项目级工作流。
- 不展示可视化 Chain-of-Thought。
- 不把短视频、AI 视频分镜提前放入执行排期。

## 当前要做什么

### 1. 建立固定测试数据集

- 新增 `test-data/` 目录。
- 将测试用例从 `tests/run-test-plan.js` 中拆出，形成可长期维护的数据集。
- 建议结构：

```text
test-data/
├─ text-cases.jsonl
├─ image-cases.jsonl
└─ edge-cases.jsonl
```

每条文本用例建议字段：

```json
{
  "id": "T001",
  "page": "text",
  "scene": "通用",
  "theme": "历史",
  "input_type": "普通口语",
  "input": "帮我讲讲秦始皇统一六国的意义",
  "expected_direction": "补充受众、分析角度、输出结构，避免泛泛介绍",
  "quality_risks": ["过度学术", "输出太泛", "结构不清"]
}
```

每条图片用例建议字段：

```json
{
  "id": "I001",
  "page": "image",
  "image_type": "通用",
  "theme": "风景",
  "input_type": "模糊画面",
  "input": "雨后的古城街道，晚上，有灯光",
  "expected_direction": "补充材质、反光、街道层次、镜头和氛围，不改变古城夜景",
  "quality_risks": ["擅自改变地点", "风格过度", "负面提示词堆砌"]
}
```

### 2. 完整归档每次测试结果

每次测试生成独立目录，不覆盖历史结果：

```text
test-results/
└─ 2026-07-26-run-001/
   ├─ summary.md
   ├─ text-cases.jsonl
   ├─ image-cases.jsonl
   ├─ failures.jsonl
   └─ artifacts/
      ├─ screenshots/
      └─ raw-responses/
```

每条结果必须保存：

- 原始输入
- 测试用例元数据
- 完整模型原始响应
- 解析后的结构化 JSON
- 最终可复制提示词
- 历史记录状态
- 质量评分
- 错误信息
- 耗时

如果没有保存完整模型输出，则该轮测试视为无效，需要重跑。

### 3. 将质量评分从“字段检查”升级为“语义评估”

当前自动评分过于依赖字段是否存在，容易出现虚高分。下一步评分必须对照：

- `expected_direction`
- `quality_risks`
- 原始用户意图
- 优化后提示词是否可直接复制

文本提示词重点看：

- 是否忠实原意
- 是否明确目标和输出结构
- 是否合理补全上下文
- 是否避免过度复杂化
- 是否适合普通用户

图片提示词重点看：

- 是否保留原始主体
- 是否补充有效视觉信息
- 是否擅自添加身份、年龄、风格、场景
- 负面提示词是否针对当前画面
- 中文/英文提示词是否能直接使用

### 4. 人工复盘低分样本

每轮测试后至少复盘：

- 所有失败用例
- 所有平均分低于 4 的用例
- 随机抽取 5 条自动评分为 5 的用例，检查是否虚高

复盘时记录：

```text
问题类型：
- JSON 不稳定
- 字段缺失
- 跑题
- 过度补全
- 过度复杂化
- 输出太长
- 不适合普通用户
- 图片负面提示词堆砌

建议改法：
- 修改 system prompt
- 修改前端解析容错
- 修改测试评分规则
- 修改数据集样本
```

### 5. 用失败样本反向优化 system prompt

优先处理当前已暴露的问题：

- T016 长文本旅行计划导致 JSON 解析失败。
- 图片人物头像类提示词容易擅自具体化年龄、种族、发型、服饰。
- 自动质量评分存在虚高。

建议 system prompt 增加规则：

- 长文本输出必须先保证 JSON 合法性，宁可减少建议数量，也不要破坏 JSON。
- 对信息缺失项，用“可选变量”或“默认假设”表达，不要擅自具体化用户未说明的身份特征。
- 简单需求不要扩写成复杂模板。
- 如果需要补充设定，必须与原始输入保持兼容，并在优化策略中说明是假设性补充。

## 轻量功能建议

这些可以进入后续排期，但不急于实现：

### A. 多版本优化

输出：

- 简洁版
- 标准版
- 增强版

价值：普通用户可以直接选择合适版本，比复杂 A/B 手术台更轻。

### B. 原文 vs 优化后 Diff

展示：

- 新增了哪些约束
- 明确了哪些输出格式
- 保留了哪些原意
- 删除或弱化了哪些模糊表达

价值：让用户理解为什么提示词变好了。

### C. 本地反馈标签

按钮：

- 好用
- 太长
- 太复杂
- 跑题
- 想要更口语

价值：积累本地偏好，不做云端 RLHF。

## 下一步执行建议

### Step 1：拆出固定测试数据

- 建 `test-data/`。
- 从 `tests/run-test-plan.js` 拆出文本、图片、边界用例。
- 补齐 `expected_direction` 和 `quality_risks`。

### Step 2：改造测试脚本

- 从 `test-data/*.jsonl` 读取用例。
- 将用例元数据写入测试结果。
- 评分时读取 `expected_direction` 和 `quality_risks`。

### Step 3：重跑测试

- 生成新一轮完整归档。
- 保留 raw response、parsed result、optimized prompt。
- 不覆盖旧结果。

### Step 4：人工复盘

- 先看失败样本。
- 再看低分样本。
- 再抽查高分样本是否虚高。

### Step 5：优化 system prompt

- 根据失败模式修改文本页和图片页 system prompt。
- 修改后再次跑同一批数据集，对比质量变化。

## 成功标准

短期成功标准：

- 测试数据从脚本中独立出来。
- 每轮测试都有完整结果归档。
- 失败样本可以追溯到完整模型响应。
- 自动评分不再大量虚高。

中期成功标准：

- 文本页大多数普通用户场景平均质量分稳定 >= 4。
- 图片页不再频繁擅自添加用户未说明的主体身份特征。
- 长文本复杂输入不再破坏 JSON。
- 每次 system prompt 修改都能通过同一数据集对比效果。

