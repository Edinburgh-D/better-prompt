# better-prompt 优化排期工作列表

> 原则：本地优先、轻量可维护、不引入重依赖。优先解决稳定性、核心体验和可复用能力，再考虑分发与商业化。

## 优先级定义

- P0：本周可完成，明显改善稳定或核心体验
- P1：下个迭代必须纳入
- P2：中长期规划，待验证

## 当前阶段目标

把 better-prompt 从“基础可用原型”升级为“稳定、可信、可持续迭代的本地提示词优化工作台”。

## 下一步执行顺序

> 给执行 agent：按下面顺序做，不要自行扩展范围。当前最重要的是把已完成的文本优化主流程收窄到大众用户定位，然后再做图片提示词优化页面。

### Step 1：校准文本页场景选项

- 状态：已完成
- 目标：把当前文本提示词优化页从偏专业/泛化模式，调整为面向普通用户的场景选项。
- 必做：
  - 保留：通用、小红书笔记、公众号文章、朋友圈文案、产品介绍、工作邮件、学习提问。
  - 移除或隐藏：代码、图像生成等不适合放在文本页的选项。
  - 同步调整本地模板库，优先覆盖上述大众场景。
- 不做：
  - 不新增独立文案页面。
  - 不做代码提示词优化入口。
  - 不把短视频放进文本页。
- 验收：
  - 页面默认仍是“通用”。
  - 用户可以选择大众场景。
  - 不同场景会影响发送给模型的提示词策略。

### Step 2：新增图片提示词优化页面

- 状态：已完成
- 目标：新增独立图片提示词优化页，和文本页分开。
- 必做：
  - 新增页面：`image-prompt.html`。
  - 如果需要新增脚本，可用 `image-prompt.js`。
  - 样式优先复用 `styles.css`，不要重做一套视觉系统。
  - 顶部或页面入口增加 `文本优化` / `图片优化` 导航。
  - 使用 `MIND.md` 中“图片提示词优化 system prompt 草案 v1.0”作为基础。
  - 图片历史记录和文本历史记录分开，例如使用 `imagePromptHistory`。
- 输出区必须支持：
  - 画面意图
  - 诊断评分
  - 主要问题
  - 优化建议
  - 中文优化提示词
  - 英文优化提示词
  - 负面提示词
  - 参数建议
  - 可选风格变体
- 不做：
  - 不接入真实图片生成。
  - 不做图床、图库、作品管理。
  - 不引入新框架、构建系统、数据库或账号体系。
- 验收：
  - 文本页和图片页互不影响。
  - 中文优化提示词、英文优化提示词、负面提示词可以分别复制。
  - JSON 解析失败时能降级展示原文。
  - API 失败时保留输入并提供重试。

### Step 3：回归验证并更新记录

- 状态：已完成
- 必做验证：
  - `python -m py_compile server.py`
  - 启动服务并访问 `http://localhost:5000`
  - 手动测试文本页：输入、优化、复制、历史记录。
  - 手动测试图片页：输入、优化、复制中文提示词、复制英文提示词、复制负面提示词、历史记录。
  - 测试失败路径：未配置或移除 `DEEPSEEK_API_KEY` 时错误提示是否清楚。
- 完成后更新：
  - 把完成的排期项状态改为“已完成”。
  - 在“工作记录”里新增日期、项目、状态、备注。
  - 未完成的检查项必须写明“未完成原因”。

## P0 本周优先事项

### 1. 修复中文编码乱码

- 状态：已完成
- 影响范围：`index.html`、`server.py`、`README.md`
- 目标收益：恢复产品第一印象，避免系统提示词乱码影响模型输出质量。
- 实施提示：
  - 统一所有源码文件为 UTF-8 编码。
  - 恢复页面标题、按钮、空状态、错误提示、README 文案和后端启动日志。
- 验收标准：
  - 浏览器页面中文显示正常。
  - README 中文显示正常。
  - 后端启动日志中文显示正常。

### 2. 建立结构化模型输出协议

- 状态：已完成
- 影响范围：前端提示词、结果解析、结果渲染
- 目标收益：避免依赖 Markdown 标题和正则拆分，提高结果展示和复制稳定性。
- 实施提示：
  - 要求模型返回固定 JSON 字段：`task_type`、`user_intent`、`strategy`、`scores`、`strengths`、`weaknesses`、`suggestions`、`optimized_prompt`、`optional_enhancements`。
  - 前端优先按 JSON 渲染，解析失败时降级展示原始文本。
- 验收标准：
  - 优化后的提示词能稳定单独展示。
  - 即使模型返回格式异常，页面也不会空白或报错中断。

### 3. 完善错误处理与用户提示

- 状态：已完成
- 影响范围：`server.py`、前端请求逻辑
- 目标收益：用户能判断问题来源并知道下一步怎么处理。
- 实施提示：
  - 后端统一返回 `{ "error": { "code": "...", "message": "..." } }`。
  - 区分 `MISSING_API_KEY`、`INVALID_REQUEST`、`UPSTREAM_ERROR`、`UPSTREAM_TIMEOUT`、`UNKNOWN_ERROR`。
  - 前端根据错误码显示可操作提示和“重试”按钮。
- 验收标准：
  - 未配置 API Key 时提示明确。
  - 服务异常或模型异常时不会覆盖用户输入。
  - 用户可以直接重试上一次请求。

### 4. 优化加载状态和复制反馈

- 状态：已完成
- 影响范围：`index.html`
- 目标收益：降低等待焦虑，提升完成后的操作流畅度。
- 实施提示：
  - 加载期间显示阶段文案和耗时。
  - 复制成功用按钮状态或 toast，不使用 `alert` 打断流程。
- 验收标准：
  - 优化中按钮不可重复点击。
  - 复制后有非阻塞反馈。

## P1 下个迭代事项

### 5. 拆分前端静态资源

- 状态：已完成
- 影响范围：`index.html`、新增 `styles.css`、新增 `app.js`
- 目标收益：降低单文件复杂度，方便后续维护。
- 实施提示：
  - 保持原生 HTML/CSS/JS，不引入构建工具。
  - 将样式迁移到 `styles.css`，业务逻辑迁移到 `app.js`。
- 验收标准：
  - Flask 访问和直接打开 HTML 的基础体验仍可用。
  - 页面样式与功能不回退。

### 6. 增加优化模式

- 状态：已完成
- 影响范围：前端控件、后端提示词选择
- 目标收益：不同场景输出更贴合，提升专业感。
- 实施提示：
  - 初版支持：通用、小红书笔记、公众号文章、朋友圈文案、产品介绍、工作邮件、学习提问。
  - 这些文案、办公、学习类场景作为同一个文本提示词优化页里的选项，不单独建页面。
  - 每个场景只维护少量差异化提示词片段，避免复杂配置系统。
- 验收标准：
  - 用户可在优化前选择使用场景。
  - 不同场景会影响模型诊断重点和优化结果。
  - 默认仍为“通用”，避免增加普通用户理解成本。

### 7. 增加输出详细度选项

- 状态：已完成
- 影响范围：前端控件、提示词协议
- 目标收益：用户可以在速度、成本和分析深度之间做选择。
- 实施提示：
  - 支持：精简版、标准版、完整诊断版。
  - 精简版只输出关键问题和优化后提示词。
- 验收标准：
  - 不同详细度对应不同输出结构。
  - 默认使用标准版。

### 8. 结构化历史记录管理

- 状态：已完成
- 影响范围：localStorage 数据结构、历史记录 UI
- 目标收益：历史记录可搜索、可复制、可二次使用。
- 实施提示：
  - 保存原始输入、结构化结果、模型名、模式、创建时间。
  - 增加单条删除、复制优化结果、重新加载。
- 验收标准：
  - 历史记录支持单条操作。
  - 旧历史数据无法解析时不影响新记录展示。

### 9. 内置提示词模板库

- 状态：已完成
- 影响范围：前端静态数据、输入区交互
- 目标收益：降低空白输入门槛，提升工具即用性。
- 实施提示：
  - 初版内置 8-12 个模板，覆盖小红书、公众号、朋友圈、产品介绍、邮件、学习提问等大众场景。
  - 模板以本地 JS 对象维护。
- 验收标准：
  - 用户能一键加载模板到输入框。
  - 模板不会替代用户当前输入，需有确认或插入机制。

### 10. 新增图片提示词优化页面

- 状态：已完成
- 影响范围：新增独立页面、前端导航、图片提示词专用 system prompt、结果展示结构、历史记录分组
- 目标收益：图片生成是大众用户更容易感知效果的场景。将图片提示词优化从文本提示词优化页中拆出，可以使用更贴合图像生成的诊断维度，提升输出可控性。
- 产品原则：
  - 图片提示词优化页独立于文本提示词优化页，不混用同一套诊断标准。
  - 页面只做“图片提示词诊断与优化”，不做图片生成、不做图床、不做复杂作品管理。
  - 保持本地优先和轻量实现，优先复用现有 Flask 代理和 `/api/optimize` 接口。
- 实施提示：
  - 页面可命名为 `image-prompt.html`。
  - 如果前端已拆分，可新增 `image-prompt.js`，样式优先复用 `styles.css`。
  - 入口可放在顶部导航：`文本优化` / `图片优化`。
  - 图片页使用 `MIND.md` 中的“图片提示词优化 system prompt 草案 v1.0”作为后续实现基础。
- 建议页面输出：
  - 画面意图
  - 诊断评分：主体清晰度、场景完整度、视觉控制、细节质量、约束清晰度、模型可用度
  - 主要问题与优化建议
  - 中文优化提示词
  - 英文优化提示词
  - 负面提示词
  - 参数建议：画幅比例、风格强度、细节程度
  - 可选风格变体
- 验收标准：
  - 文本优化页和图片优化页互不影响。
  - 图片页可以独立完成输入、优化、展示、复制。
  - 中文优化提示词、英文优化提示词、负面提示词可分别复制。
  - JSON 解析失败时，能降级展示模型原始文本并提示格式异常。
  - 图片历史记录如需实现，应和文本历史分开存储，例如 `imagePromptHistory`。
  - 不新增前端框架、构建系统、数据库或账号体系。

## P2 中长期规划

### 11. 模板导入导出

- 状态：待验证
- 待验证原因：当前模板库仍处于本地内置阶段，尚未确认用户是否有跨设备、团队共享或自定义模板迁移需求。
- 目标收益：方便个人沉淀和团队共享。
- 实施提示：
  - 前端用 JSON 文件导入导出。
  - 不做账号体系和云同步。
- 验收标准：
  - 导出的模板文件可以重新导入并恢复。

### 12. 多模型配置

- 状态：待验证
- 待验证原因：当前主目标仍是打磨 DeepSeek 兼容接口下的本地原型体验；多模型会增加配置、错误处理和用户理解成本，暂不进入近期实现。
- 目标收益：支持 DeepSeek、OpenAI 兼容接口、本地模型或企业网关。
- 实施提示：
  - 使用轻量 `config.json` 或环境变量管理模型配置。
  - 后端保留统一 `/api/optimize` 接口。
- 验收标准：
  - 用户无需改代码即可切换模型配置。

## 三个迭代周期建议

### 迭代 1：稳定性与核心体验，4-6 周

- 核心目标：让工具稳定、可信、可持续使用。
- 核心任务：
  - 完成 P0-1 中文编码修复。
  - 完成 P0-2 结构化输出协议。
  - 完成 P0-3 错误处理。
  - 完成 P0-4 加载状态和复制反馈。

### 迭代 2：场景化与复用能力，4-6 周

- 核心目标：从单次优化器升级为提示词工作台。
- 核心任务：
  - 完成 P1-5 前端静态资源拆分。
  - 完成 P1-6 优化模式。
  - 完成 P1-7 输出详细度。
  - 完成 P1-8 结构化历史记录。
  - 完成 P1-9 内置模板库。
  - 完成 P1-10 图片提示词优化页面。

### 迭代 3：分发与价值验证，4-6 周

- 核心目标：验证更高频入口和长期价值。
- 核心任务：
  - 验证 P2-11 模板导入导出。
  - 验证 P2-12 多模型配置。
  - 整理一套可复用的高质量场景模板包。

## 工作记录

| 日期 | 项目 | 状态 | 备注 |
|---|---|---|---|
| 2026-07-26 | 创建优化排期工作列表 | 已完成 | 初版用于后续逐步完善 |
| 2026-07-26 | P0-1 修复中文编码乱码 | 已完成 | 已检查页面、README、后端启动日志和系统提示词中文显示；同步更新 README 中过期配置说明 |
| 2026-07-26 | P0-2 建立结构化模型输出协议 | 已完成 | 前端已按固定 JSON 字段解析和渲染，解析失败时降级展示原始文本 |
| 2026-07-26 | P0-3 完善错误处理与用户提示 | 已完成 | 后端统一错误结构；前端按错误码展示操作建议并提供重试按钮 |
| 2026-07-26 | P0-4 优化加载状态和复制反馈 | 已完成 | 加载期间显示阶段文案和耗时；复制反馈改为按钮状态和 toast |
| 2026-07-26 | P1-5 拆分前端静态资源 | 已完成 | 已拆分为 index.html、styles.css、app.js，并补充 Flask 静态资源路由 |
| 2026-07-26 | P1-6 增加优化模式 | 已完成 | 已收窄为面向大众用户的文本场景选项：通用、小红书笔记、公众号文章、朋友圈文案、产品介绍、工作邮件、学习提问 |
| 2026-07-26 | P1-7 增加输出详细度选项 | 已完成 | 已支持精简版、标准版、完整诊断版，默认标准版，渲染逻辑兼容不同字段结构 |
| 2026-07-26 | P1-8 结构化历史记录管理 | 已完成 | 历史记录已支持搜索、单条删除、复制优化结果、重新加载，并保存模型、模式和详细度 |
| 2026-07-26 | P1-9 内置提示词模板库 | 已完成 | 模板已覆盖大众场景：小红书、公众号、朋友圈、产品介绍、邮件、学习提问、资料整理 |
| 2026-07-26 | Step 1 校准文本页场景选项 | 已完成 | 已移除/隐藏代码、图像生成入口；未新增短视频或代码入口 |
| 2026-07-26 | P1-10 新增图片提示词优化页面 | 已完成 | 已新增 image-prompt.html 和 image-prompt.js，复用 styles.css，使用 MIND.md 图片 system prompt v1.0，图片历史使用 imagePromptHistory |
| 2026-07-26 | Step 3 回归验证并更新记录 | 已完成 | 已按 TEST_PLAN 执行 Playwright 无头测试，最终归档为 44 项、43 项通过、1 项失败，报告见 test-results/2026-07-26-run-001/summary.md |
| 2026-07-26 | TEST_PLAN 稳定性测试 | 已完成 | 文本页 20 个场景、图片页 16 个类型、基础访问和失败路径已测试；T016 因模型返回非法 JSON 失败 |
| 2026-07-26 | 测试归档结构完善 | 已完成 | 已生成 summary.md、text-cases.jsonl、image-cases.jsonl、failures.jsonl、artifacts/screenshots、artifacts/raw-responses；未保存 API Key、请求头或敏感环境变量 |

## 交接与防错检查清单

> 给后续执行 agent 使用：每次改动前先读本节，改动后按检查项逐条确认。不要为了完成某个功能引入复杂框架、构建系统或云端依赖。

### 执行前检查

- [x] 确认当前工作目录是 `D:\better-prompt`，不要修改目录外文件。
- [x] 先查看 `git status`，识别是否已有用户或其他 agent 的未提交改动。
- [x] 不要回滚、覆盖或格式化与当前任务无关的文件。
- [x] 每次只处理一个明确任务，例如先做 `P0-1`，不要同时混改多个功能。
- [x] 若涉及中文文案或提示词，必须确认文件以 UTF-8 保存，避免再次产生乱码。
- [x] 保持技术栈轻量：原生 HTML/CSS/JS + Flask；除非排期明确要求，不新增前端框架、打包器、数据库或账号体系。

### 编码与文案检查

- [x] 页面标题、按钮、输入框 placeholder、空状态、错误提示均为正常中文。
- [x] `README.md` 中文显示正常，命令和环境变量说明准确。
- [x] `server.py` 启动日志中文显示正常，字符串闭合完整，不能出现语法错误。
- [x] 系统提示词不得包含乱码、残缺 Markdown 标记或无法识别的占位符。
- [x] 如果重写提示词，保留“不要过度复杂化简单任务”和“优化后提示词可直接复制使用”的约束。

### 功能回归检查

- [x] 直接访问 `http://localhost:5000` 可以打开页面。
- [x] 直接打开 `index.html` 时，前端仍会请求 `http://localhost:5000/api/optimize`。
- [x] 输入为空时不会发送请求，并给出明确提示。
- [x] 点击“开始优化”后按钮禁用，完成或失败后按钮恢复。
- [x] API 成功返回后，优化后的完整提示词能单独展示并可复制。
- [x] localStorage 历史记录仍能保存，最多保留 20 条或按新规则明确处理。
- [x] 清空历史记录前有确认机制。

### 结构化输出检查

- [x] 如果改为 JSON 输出，前端必须处理 JSON 解析失败的降级情况。
- [x] 模型返回字段缺失时，页面不能空白或抛出未捕获异常。
- [x] `optimized_prompt` 字段必须优先用于复制，不要从已渲染 HTML 中反向提取。
- [x] 保存历史记录时，应保存原始结构化数据，而不是只保存 HTML 字符串。
- [x] 不要把 API Key、完整请求头或敏感错误堆栈写入历史记录或页面。

### 测试归档检查

- [x] 每次 TEST_PLAN 执行结果按 `test-results/YYYY-MM-DD-run-XXX/` 单独归档。
- [x] 已生成 `summary.md`。
- [x] 已生成 `text-cases.jsonl`，并保存文本页完整模型原始响应、结构化 JSON、`optimized_prompt`、历史状态、质量检查结果、错误信息和耗时。
- [x] 已生成 `image-cases.jsonl`，并分别保存 `optimized_prompt_cn`、`optimized_prompt_en`、`negative_prompt`。
- [x] 已生成 `failures.jsonl`，失败路径和低分样本保留原始响应与页面错误信息。
- [x] 已生成 `artifacts/screenshots/` 和 `artifacts/raw-responses/`。
- [x] 未在测试归档中保存 API Key、请求头或敏感环境变量。
- [ ] 人工逐条语义评分未完成。执行失败原因：该项需要人工判断忠实原意、克制程度和负面提示词适配度，当前自动化仅生成复盘材料。

### 后端接口检查

- [x] `/api/optimize` 只接受必要参数，不盲目信任前端传入的任意模型配置。
- [x] 未配置 `DEEPSEEK_API_KEY` 时返回明确错误码，不尝试调用上游 API。
- [x] 上游接口异常时返回统一错误结构，例如 `{ "error": { "code": "...", "message": "..." } }`。
- [x] 对超时、网络异常、模型返回异常分别给出可理解的错误信息。
- [x] 不要在日志中打印 API Key 或用户完整提示词，除非用户明确要求调试。

### 体验与界面检查

- [x] 复制成功反馈不使用阻塞式 `alert`，优先使用按钮状态或轻量 toast。
- [x] 错误提示必须告诉用户下一步操作，例如“检查服务是否启动”或“配置 API Key”。
- [ ] 移动端宽度下输入区、结果区和历史区不能横向溢出。未完成原因：本次 TEST_PLAN 脚本未包含移动端视口截图断言。
- [ ] 长提示词、长代码块、长模型结果不会撑破布局。未完成原因：本次 TEST_PLAN 脚本未包含长内容真实渲染压力测试。
- [x] 不使用大规模装饰性改版，避免把本地工具做成复杂 SaaS 落地页。

### 验证命令建议

- [x] 运行 Python 语法检查：`python -m py_compile server.py`
- [x] 启动本地服务：`python server.py`
- [x] 浏览器访问：`http://localhost:5000`
- [x] 手动测试一次成功路径：输入提示词、优化、复制、查看历史。
- [x] 手动测试一次失败路径：临时移除或不配置 `DEEPSEEK_API_KEY`，确认错误提示清楚。

### 交付说明检查

- [x] 说明本次完成了哪些排期项。
- [x] 说明修改了哪些文件。
- [x] 说明做过哪些验证。
- [x] 如果有未完成项、兼容性问题或需要用户配置的事项，必须明确写出。
