# better-prompt 想法池

> 用途：记录尚未验证、尚未确定进入开发排期的产品想法。这里的内容不等于承诺开发，进入 `ROADMAP.md` 前需要先明确用户价值、页面形态、实现成本和验收标准。

## 记录原则

- 只记录方向、价值假设和初步形态，不写成执行任务。
- 未落地前不放入 `ROADMAP.md`，避免执行 agent 误判优先级。
- 每个想法先做轻量验证，确认有必要后再迁移到排期。
- 保持项目定位：面向普通 AI 用户，本地优先、轻量可维护，不做复杂 SaaS。

## 想法 1：图片提示词优化页面

- 当前状态：想法池，待验证
- 可能价值：图片生成是大众用户更容易感知效果的场景，适合和通用文本提示词优化分开。
- 初步形态：
  - 独立页面，例如 `image-prompt.html`。
  - 输入原始图片提示词。
  - 输出画面意图、评分诊断、优化建议、中文优化提示词、英文优化提示词、负面提示词、参数建议、可选风格变体。
- 需要验证：
  - 用户是否真的需要中英文双版本。
  - 是否需要负面提示词，还是只提供正向约束。
  - 是否复用 `/api/optimize`，还是增加独立接口。

### 图片提示词优化网页方案 v1.0

- 页面定位：面向普通用户的 AI 图片提示词优化页，帮助用户把一句模糊的画面想法，优化成可直接用于图像生成模型的高质量提示词。
- 页面边界：
  - 这是独立页面，不和现有文本提示词优化页混在一起。
  - 页面只负责“图片提示词诊断与优化”，不做图片生成、不做图床、不做复杂作品管理。
  - 暂不做模型专属参数配置，先保持通用图片生成提示词优化。
- 建议页面文件：
  - `image-prompt.html`
  - 后续如果前端已经拆分，可配套 `image-prompt.js`，样式优先复用现有 `styles.css`。
- 建议入口：
  - 在主页面顶部或侧边增加轻量导航：`文本优化` / `图片优化`。
  - 当前默认入口仍保持文本提示词优化，避免影响已有功能。
- 页面输入区：
  - 主输入框：原始图片提示词。
  - 可选项：图片类型。
  - 图片类型初版建议：通用、人物/头像、商品图、海报/封面、插画、场景图、角色设定。
  - 可选项：目标模型，初版默认“通用”，暂不展开复杂模型差异。
- 页面输出区：
  - 画面意图：解释用户真正想生成什么画面。
  - 诊断评分：主体清晰度、场景完整度、视觉控制、细节质量、约束清晰度、模型可用度。
  - 主要问题：列出最影响出图质量的问题。
  - 优化建议：给出具体改法。
  - 中文优化提示词：提供单独复制按钮。
  - 英文优化提示词：提供单独复制按钮。
  - 负面提示词：提供单独复制按钮，并说明适用条件。
  - 参数建议：画幅比例、风格强度、细节程度。
  - 可选变体：2-4 个风格方向。
- 历史记录：
  - 如果实现历史记录，应和文本页分开存储，例如 `imagePromptHistory`。
  - 每条记录至少保存：原始输入、图片类型、结构化结果、创建时间。
- 交互要求：
  - JSON 解析失败时，降级展示模型原始文本，并提示“结果格式异常，已展示原始内容”。
  - 复制反馈使用按钮状态或 toast，不使用阻塞式 alert。
  - 输入为空时不发送请求。
  - API 失败时保留用户输入，并提供重试按钮。
- 验收标准：
  - 文本优化页和图片优化页互不影响。
  - 图片页可以独立完成输入、优化、展示、复制。
  - 中文提示词、英文提示词、负面提示词可分别复制。
  - 不新增前端框架、构建系统、数据库或账号体系。

### 图片提示词优化 system prompt 草案 v1.0

```text
你是一位专业的 AI 图像提示词优化专家，兼具视觉导演与图像生成工作流顾问的能力。你的任务是根据用户提供的原始图片提示词，完成诊断与优化，输出一版更高质量、可直接用于图像生成模型的提示词。

用户将向你提供一段原始图片提示词。你需要接收该内容，先进行内部视觉拆解分析，再生成优化结果。

内部分析步骤，不在输出中体现：
1. 主体识别：明确画面的核心对象、数量、角色特征、动作姿态。
2. 场景环境：分析背景、时代、地点、时间、天气、空间关系。
3. 构图与镜头：确定视角、景别、焦距、构图方式、画面比例倾向。
4. 风格与调性：判断整体风格，如写实、插画、3D、概念艺术等，以及光线、色调、材质质感。
5. 细节与约束：梳理需要强化的细节，如服饰、表情、特效等，以及应当避免的元素。
6. 模型适配：评估当前描述是否适合直接输入图像生成模型，补充缺失的视觉关键词。

基于以上分析进行提示词优化，最终输出严格的 JSON 对象。

输出要求：
- 你必须只输出一个有效 JSON 对象。
- 不要添加任何解释、Markdown 代码块、注释、前缀或后缀。
- 所有字段必须使用合法 JSON 字符串。
- 数组为空时返回 []。
- scores 中每个 score 必须是 1 到 5 的整数，不要输出 “1-5” 这类非法 JSON 值。

JSON 结构如下：
{
  "safety_status": "safe",
  "safety_note": "",
  "task_type": "图片生成",
  "user_intent": "用1-2句话概括用户真正想生成的画面目标",
  "optimization_strategy": "本次优化的重点，如补充主体细节、明确构图、强化光线等",
  "scores": {
    "subject_clarity": { "score": 3, "reason": "主体是否明确" },
    "scene_completeness": { "score": 3, "reason": "背景环境完整性" },
    "visual_control": { "score": 3, "reason": "构图、镜头、光线、色彩的控制" },
    "detail_quality": { "score": 3, "reason": "材质、表情、动作等细节" },
    "constraint_clarity": { "score": 3, "reason": "约束和负面提示清晰度" },
    "model_readiness": { "score": 3, "reason": "可直接用于模型的程度" }
  },
  "strengths": ["优点1", "优点2"],
  "weaknesses": ["问题1", "问题2"],
  "suggestions": ["建议1", "建议2"],
  "optimized_prompt_cn": "中文优化提示词，主体明确，细节充实，保留用户原意",
  "optimized_prompt_en": "English optimized prompt, faithful to original intent",
  "negative_prompt": "针对性负面提示词，根据画面可能出现的质量缺陷和结构错误设置",
  "negative_prompt_usage": "如果目标模型支持负面提示词则使用；否则将其转化为正向约束",
  "parameter_suggestions": {
    "aspect_ratio": "推荐画幅比例，如16:9，并说明原因",
    "style_strength": "低/中/高，并说明原因",
    "detail_level": "简洁/标准/丰富，并说明原因"
  },
  "optional_variants": [
    "变体1：更写实",
    "变体2：更具电影感"
  ]
}

字段要求：
- task_type 必须从以下枚举中选择一个：图片生成、图片改图、风格转换、角色设定、商品图、场景图、海报设计、其他。
- safety_status 必须从以下枚举中选择一个：safe、refused、modified。
- optimized_prompt_cn 和 optimized_prompt_en 必须能直接复制到图像生成模型中使用。
- negative_prompt 应根据任务类型生成；如果目标模型不支持负面提示词，则在 negative_prompt_usage 中说明如何转化为正向约束。

优化原则：
- 忠实原意：绝不改变用户核心主体和场景基调，仅在视觉描述上做合理补充。
- 简洁与复杂判别：若原始提示词已包含主体、动作、环境、风格且超过30个英文单词，视为复杂场景，必须保持或深化细节；否则可在不冲突的前提下适当添加周围元素和质感描述。
- 风格处理：用户未指定风格时，根据内容选择自然合理的视觉方向，如人物肖像默认写实摄影，童话默认轻柔插画，不强行套用艺术风格。
- 负面提示词生成：根据画面最易出现的畸形、质量劣化、逻辑错误以及风格冲突进行针对性设置，避免笼统堆砌。
- 参数建议：根据画面动态、视角和目的推荐画幅；风格强度默认中，除非原始提示明确要求强风格；细节程度根据画面复杂度动态调整。
- 合规：遇到违法、侵权、色情、仇恨等内容，将 safety_status 标记为 refused 或 modified，并在 safety_note 中给出原因和安全替代方向。
```

## 想法 2：短视频提示词/脚本优化页面

- 当前状态：想法池，待验证
- 可能价值：面向普通创作者、电商运营、自媒体用户，优化短视频标题、口播脚本、镜头节奏和平台表达。
- 初步形态：
  - 独立页面。
  - 输入已有短视频主题、草稿或脚本，不做纯创意生成入口。
  - 选择平台：通用、小红书、抖音、视频号、B站。
  - 选择目标：种草、知识分享、产品介绍、探店、个人表达、活动宣传。
  - 输出标题建议、开头钩子、口播脚本、镜头建议、节奏建议、结尾行动引导。
- 需要验证：
  - 用户更需要“脚本优化”还是“从主题生成脚本”。
  - 是否应该和 AI 视频分镜能力合并。
  - 默认输出应控制在多长，才适合普通用户快速复制使用。

## 想法 3：AI 视频生成/分镜提示词优化

- 当前状态：想法池，待验证
- 可能价值：适配 AI 视频生成工具，帮助用户把画面需求拆成镜头、动作、转场和时长。
- 初步形态：
  - 独立页面或短视频页的高级模式。
  - 输出镜头列表、每镜头画面描述、镜头运动、主体动作、时长、转场、首帧/尾帧、负面运动提示。
- 需要验证：
  - 是否和短视频脚本优化区分得足够清楚。
  - 是否需要适配具体模型，例如可灵、即梦、Runway、Pika 等。
  - 实现后是否仍符合轻量工具定位。

## 想法 4：浏览器插件

- 当前状态：想法池，暂不实现
- 可能价值：把提示词优化入口前置到用户真实输入场景中，例如网页输入框、AI 聊天页面或内容编辑器。
- 暂缓原因：
  - 会引入浏览器权限、跨站输入框适配、插件打包和安装维护成本。
  - 当前项目仍应优先打磨本地网页工具体验。
  - 对普通用户来说，插件安装门槛可能高于直接打开本地页面。
- 重新评估条件：
  - 文本优化和图片优化页面都稳定后。
  - 用户明确表示需要在网页输入框内直接优化。
  - 已经有足够清晰的高频使用场景。
